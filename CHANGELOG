R2
Allow choosing syncing output nodes by frame or by timestamp
Accept -l/--lib argument to set vsscript lib search directory on launch
Set env variables for loading custom vsscript library from portable VS
Accept -o/--outputindex for default output index (0-9) like in vspipe
Change -fp/--force-portable argument to -p/--portable

R1
Initial release, stripped from VapourSynth-Editor R19-mod-6.3
Show core cache usage in status bar
Add experimental cache clearing mechanism when changing frames
Remove 2020_CL option in preview advanced settings
Fix duplicated VS messages sent from core
Fix filter message not shown before full evaluation
Fix each frame from script requested twice
Accept -a/--arg arguments like in vspipe
Accept -fp/--force-portable argument to force portable mode on launch
Add experimental audio playback
